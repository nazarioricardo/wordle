export const selectActiveIndex = (state) => state.word.index;
export const selectCurrentWord = (state) => state.word.current;
export const selectGuesses = (state) => state.word.guesses;
export const selectTargetWord = (state) => state.word.target;
export const selectResults = (state) => state.word.results;
